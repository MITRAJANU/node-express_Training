const port = process.env.PORT || 3000;

console.log(`Hello from the Backend Masterclass skeleton on port ${port}`);
console.log("Run `git checkout checkpoint-01-js-refresher` when class moves to the first checkpoint.");
